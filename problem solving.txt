هذا الملف مخصص فقط لعرض المشاكل المعترضة وحلولها.

1. استخراج القيم من معجم داخل القالب:
    _  نقوم أولا بإنشاء فلتر للقيام بذلك
        from django import template
        register = template.Library()
        @register.filter
        def get_item(dictionary, key):
            return dictionary.get(key)
    _ نستدعي الفلتر في القالب:
        <small> مجموع الغياب خلال السنة {% with yearCount|get_item:year as total %} {{total}}  {% endwith %} </small>


--------------------------------------------------------------------------------------------------------------------------        
2.: على السنة من أجل الوصول إلى الشهر ثم على هذا الأخير من أجل الوصول إلى عدد غياباته get_item اعترضتني مشكلة عند حساب عدد غيابات الشهر حيث ستختلط الغيابات حسب عدد السنوات لذلك اضررت إلى تعديل القاموس الخاص بحساب غيابات الشهر حيث أضفت له السنة كمفتاح لتمييز الغيابات المختلفة حسب السنوات ثم قمت بتطبيق الفلتر 
            monthCount = {}
            for year, months in grouped_absence.items():
                totalAbsences = 0
                monthCount[year] = {}
                for month, absences in months.items():
                    totalAbsences += len(absences)
                    monthCount[year][month] = len(absences)
                yearCount[year] = totalAbsences

            <small> مجموع الغياب خلال الشهر {% with monthCount|get_item:year|get_item:month as total %} {{total}} {% endwith %}  </small>



--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
3. الذي يحل المشكل الثاني min-width:100% و  scroll الذي يحل المشكل الأول ويضيف  overflow-x = auto  المشكل يكمن في أنه عندما تصغر الصفحة فإن الجدول يتجاوزها وعندما تكبر الصفحة فمساحة فارغة بيضاء والحل يكمن في 
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
        <div style="overflow-x: auto; width: 100%;">                
            <table class="table table-hover table-bordered mb-0" style="width: auto; min-width: 100%;">



---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
4. ثم نضيف واحد count() يوجد مشكل وهو كيف نعد الغياب الخاص بتلميذ محدد بشكل ترتيبي يتم حل هذا المشكل بواسطة إضافة حقل العداد في قاعدة البيانات وكلما أردنا تخزين غياب جديد نعد عن الغيابات السابقة للتلميذ المعني باستعمال دالة 
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
    def save(self, *args, **kwargs):
        if not self.counter:
            self.counter = Absence.objects.filter(student = self.student).count() + 1
        super().save(*args, **kwargs)



---------------------------------------------------------------------------
5. view يضم داخله القيم مستوردة من ملف  select يوجد مشكل وهو كيف أضيف 
---------------------------------------------------------------------------
    <select name="material" class="form-select form-control" id="">
        {% for m in material %}
        <option value="{{m.id}}" >{{m.name}}</option>
        {% endfor %}
    </select>
    material_id =  request.POST.get('material')
    material = Subject.objects.get(id = material_id)
    Report.objects.create(
        material = material,
    )